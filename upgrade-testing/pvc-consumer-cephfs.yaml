apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  generateName: consumer-pvc-cephfs
  name: consumer-pvc-cephfs
  namespace: ocs-client-operator-system
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: storageclassclaim-sample-filesystem
  volumeMode: Filesystem

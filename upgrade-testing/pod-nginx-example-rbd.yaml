---
apiVersion: v1
kind: Pod
metadata:
  name: nginx-example-pvc-rbd
  labels:
    app: example-nginx-pvc-rbd
spec:
  volumes:
    - name: task-pv-storage
      persistentVolumeClaim:
        claimName: example-pvc-rbd
  containers:
    - name: task-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: task-pv-storage
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: example-nginx-pvc-rbd
  name: nginx-example-pvc-rbd
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: example-nginx-pvc-rbd
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: example-nginx-pvc-rbd
  name: nginx-example-pvc-rbd
spec:
  host: nginx-example-pvc-rbd-ocs-operator-system.apps.sno-dev.jarrpa.dev
  port:
    targetPort: 80
  to:
    kind: Service
    name: nginx-example-pvc-rbd
    weight: 100
  wildcardPolicy: None

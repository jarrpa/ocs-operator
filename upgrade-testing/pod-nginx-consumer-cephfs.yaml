---
apiVersion: v1
kind: Pod
metadata:
  name: nginx-consumer-pvc-cephfs
  labels:
    app: consumer-nginx-pvc-cephfs
spec:
  volumes:
    - name: task-pv-storage
      persistentVolumeClaim:
        claimName: consumer-pvc-cephfs
  containers:
    - name: task-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: task-pv-storage
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: consumer-nginx-pvc-cephfs
  name: nginx-consumer-pvc-cephfs
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: consumer-nginx-pvc-cephfs
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: consumer-nginx-pvc-cephfs
  name: nginx-consumer-pvc-cephfs
spec:
  host: nginx-consumer-pvc-cephfs-ocs-operator-system.apps.sno-dev.jarrpa.dev
  port:
    targetPort: 80
  to:
    kind: Service
    name: nginx-consumer-pvc-cephfs
    weight: 100
  wildcardPolicy: None
